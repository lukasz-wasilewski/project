'use strict';

describe('myApp.friends_list module', function() {

  beforeEach(module('myApp.friends_list'));

  describe('friends_list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});