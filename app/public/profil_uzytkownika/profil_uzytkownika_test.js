'use strict';

describe('myApp.profil_uzytkownika module', function () {

    beforeEach(module('myApp.profil_uzytkownika'));

    describe('profil_uzytkownika controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view2Ctrl = $controller('ProfilUzytkownikaCtrl');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});